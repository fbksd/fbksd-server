name: fbksd-ctl
version: "1.0"
author: Jonas Deyson <kbknapp@gmail.com>
about: Server fkbsd task manager.

subcommands:
    - validate-ci:
        about: validate the CI yml config file
    - install:
        about: build and install
    - run:
        about: run benchmark
    - publish:
        about: publish results
    - delete-workspace:
        about: delete an unpublished workspace
